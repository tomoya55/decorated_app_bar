name: decorated_app_bar
description: Customize app bar with any decoration whatever you like. Notice that we follow the same version policy with flutter framework, so that you won't get any inconsistency with the official AppBar.
version: 1.22.4
author: Tom Hirano <tom@nicecabbage.com>
homepage: https://github.com/tomoya55/decorated_app_bar

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: '>=2.7.0 <3.0.0'

flutter:
